#include "test_runner.h"

int main() {
    // RUN_ALL_TESTS() will execute all tests registered via the TEST_CASE macro.
    // It returns 0 if all tests pass, and 1 otherwise.
    return RUN_ALL_TESTS();
}
